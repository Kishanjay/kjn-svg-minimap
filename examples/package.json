{
  "name": "examples",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "start": "parcel index.html",
    "dev": "npm --prefix .. install && npm --prefix .. run watch && npm start",
    "build": "npm --prefix .. install && npm --prefix .. build"
  },
  "author": "Kishan Nirghin",
  "license": "ISC",
  "devDependencies": {
    "@kjn/svg-minimap": "file:..",
    "parcel": "^2.5.0"
  }
}
